# 控制台日志 (Logger)

在控制台中实时查看日志，支持 WebSocket 推送更新。

## 依赖

- `console`

## 配置项

该插件没有配置项。

> 该插件使用内置组件 Logger，记录日志请查看 Logger API。

## 控制台入口

插件会在控制台中新增「日志」页面。

## API 接口

> 以下接口需要登录控制台后才能访问。

- `GET /api/logger`  
  返回当前缓存的日志列表（JSON）。

## 实时日志

控制台页面会通过 WebSocket 订阅日志事件，日志产生时实时推送。
