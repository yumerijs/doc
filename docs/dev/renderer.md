# 渲染器 （实验）

为实现前后端耦合，Yumeri 提供了一个渲染器接口，可用于将 Vue / React 组件渲染为前端并进行数据挂载。

> 本功能仍在测试。由于前端框架的特殊性，水合流程可能无法正常实现。

## 使用

安装对应的渲染器。目前提供的渲染器有 @yumerijs/vue-renderer，用于渲染 Vue 组件。

然后，为了让框架识别你的渲染器选择，你需要在插件入口导出一个名为 renderer 的变量，此时我们赋值为'vue'。

接着，按照正常流程进行 vite 配置以及组件导入。